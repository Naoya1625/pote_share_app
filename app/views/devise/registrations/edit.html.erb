
<div class="setting-wrap">
  <div class="setting-left">
    <div class="setting-left-content">
      <ul>
        <li><%= link_to t('.account'), users_account_path %></li>
        <li><%= link_to t('.profile'), users_profile_url, id:"setting-toggle" %></li>
      </ul>
    </div>
  </div>

  <div class="setting-right">
    <div class="setting-right-content">
      <%= form_with model: @user, url: user_registration_path,local: true do |f| %>
        <ul>
          <li class="border-bottom-gray" id="setting-title">
            <span><%= t('.profile') %></span>
            <span></span>
          </li>

          <li class="border-bottom-gray font-bold" id="setting-user-image">

                        
              <% if @user.image.attached? %>
                <%= image_tag @user.display_image %>
              <% else %>
                <i class="setting-user-icon fas fa-user-circle"></i>
              <% end %>


          </li>

          <li class="border-bottom-gray font-bold">
            <span><%= t('.icon-image') %> <div class="required">必須</div></span>
            <span><%= f.file_field :image, accept: "image/jpeg,image/gif,image/png" %></span>
          </li>

          <li class="border-bottom-gray font-bold">
            <span><%= t('.name') %> <div class="required">必須</div></span>
            <span><%= f.text_field :name, class:"#", id:"aaa" , value: current_user.name %></span>
          </li>

          <li class="border-bottom-gray font-bold">
            <span><%= t('.introduction') %> <div class="required">必須</div></span>
            <span><%= f.text_area :introduction, class:"#", id:"aaa" , value: current_user.introduction, placeholder:"自己紹介を入力" %></span>
          </li>

          <li id="setting-submit-btn">
            <span><%= f.submit t('.update'), class:"submit-btn-profile" %></span>
            <span></span>
          </li>
        </ul>
      <% end %>
    </div>  
  </div>








</div>



<!-- ここからは旧html -->
<%
=begin%>
<div class="setting-wrap">

  <div class="setting-left">
    <ul>
      <li><%= link_to t('.account'), users_account_path, class:"setting-toggle" %></li>
      <li><%= link_to t('.profile'), users_profile_url, class:"setting-toggle select-account" %></li>
    </ul>
  </div>

  <div class="setting-right">
    <div class="profile-table">
      <%= form_with model: @user, url: user_registration_path,local: true do |f| %>
        <table>
          <tr class="table-title tr-border-bottom">
            <th><%= t('.profile') %></th>
            <td></td>
          <tr class="icon-table tr-border-bottom">
            <th colspan="2" class="round-image">                
            <% if @user.image.attached? %>
              <%= image_tag @user.image %>
            <% else %>
              <i class="setting-user-icon fas fa-user-circle"></i>
            <% end %>
            </th>
          </tr>

          <tr class="tr-border-bottom margin-top-10">
            <th><%= t('.icon-image') %> <div class="required">必須</div></th>
            <td><span class="image"><%= f.file_field :image, accept: "image/jpeg,image/gif,image/png" %></span></td>
          </tr>

          <tr class="tr-border-bottom">
            <th><%= t('.name') %> <div class="required">必須</div></th>
            <td>
              <div class="left-justified">
                <%= f.text_field :name, class:"form-field", id:"full-width" , value: current_user.name %>
              </div>
            </td>
          </tr>

          <tr class="tr-border-bottom text-area-field">
            <th><%= t('.introduction') %> <div class="required">必須</div></th>
            <td>
              <div class="left-justified">
                <%= f.text_area :introduction, class:"form-field", id:"full-width" , value: current_user.introduction, placeholder:"自己紹介を入力" %>
              </div>
            </td>
          </tr>

          <tr class="btn-table">
            <th><%= f.submit t('.update'), class:"table-btn" %></th>
            <td></ td>
          </tr>
        </table>
      <% end %>
    </div>  
  </div>
</div>

<% 
=end
%>