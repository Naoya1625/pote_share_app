require 'rails_helper'

#sessionの登録、更新、削除を行うコントローラ（ログイン状況を操作)
RSpec.describe Users::SessionsController, type: :controller do
  describe "new" do
    #ログイン画面
    context "a logged in user" do

    end
    context "as a not logged in user" do

    end
  end

  describe "create" do
    #POST
    context "a logged in user" do

    end
    context "as a not logged in user" do

    end
  end

  describe "destroy" do
    context "a logged in user" do

    end
    context "as a not logged in user" do

    end
  end
end
